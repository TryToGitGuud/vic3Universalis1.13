# ============================================================
# GREAT WAR MOD - Alignment Scripted Buttons
# Auto-split from larger scripted files for readability.
# ============================================================

gw_alignment_allied_button = {
	name = "gw_alignment_allied_button"
	desc = "gw_alignment_allied_button_desc"

	visible = {
		is_ai = no
		OR = {
			is_country_type = recognized
			is_country_type = colonial
		}
		gw_is_major_or_greater = yes
		NOT = { has_global_variable = gw_great_war_active }
	}

	possible = {
		custom_tooltip = {
			text = gw_join_allied_tooltip
			NOT = { has_variable = gw_player_allied }
			NOT = { has_variable = gw_stance_cooldown }
		}
	}

	effect = {
		gw_apply_allied_alignment = yes
		set_variable = {
			name = gw_stance_cooldown
			years = 1
		}
		custom_tooltip = { text = gw_alignment_allied_effect_tt }
	}

	ai_chance = {
		value = 0
	}
}

gw_alignment_neutral_button = {
	name = "gw_alignment_neutral_button"
	desc = "gw_alignment_neutral_button_desc"

	visible = {
		is_ai = no
		OR = {
			is_country_type = recognized
			is_country_type = colonial
		}
		gw_is_major_or_greater = yes
		NOT = { has_global_variable = gw_great_war_active }
		NOT = { has_variable = gw_player_neutral }
		NOT = { has_variable = gw_alignment_neutral }
	}

	possible = {
		custom_tooltip = {
			text = gw_neutrality_tooltip
			NOT = { has_variable = gw_stance_cooldown }
		}
	}

	effect = {
		gw_apply_neutral_alignment = yes
		set_variable = {
			name = gw_stance_cooldown
			years = 1
		}
		custom_tooltip = { text = gw_alignment_neutral_effect_tt }
	}

	ai_chance = {
		value = 0
	}
}

gw_alignment_central_button = {
	name = "gw_alignment_central_button"
	desc = "gw_alignment_central_button_desc"

	visible = {
		is_ai = no
		OR = {
			is_country_type = recognized
			is_country_type = colonial
		}
		gw_is_major_or_greater = yes
		NOT = { has_global_variable = gw_great_war_active }
	}

	possible = {
		custom_tooltip = {
			text = gw_join_central_tooltip
			NOT = { has_variable = gw_player_central }
			NOT = { has_variable = gw_stance_cooldown }
		}
	}

	effect = {
		gw_apply_central_alignment = yes
		set_variable = {
			name = gw_stance_cooldown
			years = 1
		}
		custom_tooltip = { text = gw_alignment_central_effect_tt }
	}

	ai_chance = {
		value = 0
	}
}

gw_alignment_revoke_neutrality_button = {
	name = "gw_alignment_revoke_neutrality_button"
	desc = "gw_alignment_revoke_neutrality_button_desc"

	visible = {
		is_ai = no
		has_variable = gw_player_neutral
		NOT = { has_global_variable = gw_great_war_active }
	}

	possible = {
		custom_tooltip = {
			text = gw_revoke_neutrality_tooltip
			NOT = { has_variable = gw_stance_cooldown }
		}
	}

	effect = {
		remove_variable = gw_alignment_neutral
		remove_variable = gw_player_neutral
		if = {
			limit = { has_modifier = gw_neutral_nation }
			remove_modifier = gw_neutral_nation
		}
		set_variable = {
			name = gw_stance_cooldown
			years = 1
		}
		custom_tooltip = { text = gw_alignment_revoke_effect_tt }
	}

	ai_chance = {
		value = 0
	}
}

